@model IEnumerable<FrontEnd.Models.TipoAsistencia>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipo de Asistencia</h1>

<p>
    <a asp-action="Create">Crear nuevo</a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoAsistencia1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoAsistencia1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoAsistencia">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoAsistencia">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoAsistencia">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
