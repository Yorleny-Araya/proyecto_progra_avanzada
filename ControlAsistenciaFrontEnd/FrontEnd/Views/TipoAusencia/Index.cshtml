@model IEnumerable<FrontEnd.Models.TipoAusencia>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipo de Ausencia</h1>

<p>
    <a asp-action="Create">Crear nuevo</a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoAusencia1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoAusencia1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoAusencia">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoAusencia">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoAusencia">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
