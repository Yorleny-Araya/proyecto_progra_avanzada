@model IEnumerable<FrontEnd.Models.Autenticacion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contrasena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmpleadoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdRolNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contrasena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleadoNavigation.Correo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdRolNavigation.Rol1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAutenticacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAutenticacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAutenticacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
